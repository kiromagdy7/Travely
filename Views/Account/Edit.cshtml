@model Travely.ViewModels.ProfileEditViewModel

@{
    ViewData["Title"] = "Edit Profile";
    Layout = "_Layout";
}

<div class="profile-page-container">

    <aside class="sidebar">
        <div class="user-profile">
            @if (!string.IsNullOrEmpty(Model.CurrentImagePath))
            {
                <img src="@Model.CurrentImagePath" alt="Profile Picture" class="profile-avatar-image">
            }
            else
            {
                <div class="user-avatar">@(Model.Fullname.Substring(0, 1))</div>
            }
        </div>

        <nav class="sidebar-nav">
            <ul>
                <li><a asp-action="Profile" asp-controller="Account"><i class="fas fa-briefcase"></i> My Bookings</a></li>
                <li><a asp-action="Edit" asp-controller="Account" class="active"><i class="fas fa-user"></i> Personal Info</a></li>
                <li><a asp-action="Wishlist" asp-controller="Account"><i class="fas fa-heart"></i> My Wishlist</a></li>
            </ul>
        </nav>

        <div class="logout-link">
            <a asp-action="Logout" asp-controller="Account"><i class="fas fa-sign-out-alt"></i> Log Out</a>
        </div>
    </aside>

    <main class="main-content">
        <form asp-action="Edit" method="post" enctype="multipart/form-data">
            @Html.AntiForgeryToken()

            @if (TempData["SuccessMessage"] != null)
            {
                <div class="alert alert-success">@TempData["SuccessMessage"]</div>
            }

            @Html.ValidationMessageFor(m => m.NewImage, "", new { @class = "alert alert-fail text-danger" })

            <div class="form-group mb-2 mt-0">
                <label asp-for="Fullname" class="control-label"></label>
                <input asp-for="Fullname" class="form-control" />
                <span asp-validation-for="Fullname" class="text-danger"></span>
            </div>

            <div class="form-group mb-2">
                <label asp-for="Email" class="control-label"></label>
                <input asp-for="Email" class="form-control" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>

            <div class="form-group mb-3">
                <label asp-for="Country" class="control-label"></label>

                <select asp-for="Country"
                        class="country-select"
                        asp-items="@ViewBag.Countries"
                        data-placeholder="Select your country...">
                </select>
                <span asp-validation-for="Country" class="text-danger"></span>
            </div>

            <div class="form-group mb-2">
                <label asp-for="Age" class="control-label"></label>
                <input asp-for="Age" class="form-control" type="number" />
            </div>

            <div class="form-group mb-2">
                <label asp-for="Phone" class="control-label"></label>
                <input asp-for="Phone" class="form-control" />
            </div>

            <hr />
            <div class="form-group mb-2">
                <label asp-for="NewImage" class="control-label"></label>
                <p class="mb-1 mt-2">Must be 4MB or smaller</p>
                <input asp-for="NewImage" class="form-control" type="file" accept="image/*" />
                <span asp-validation-for="NewImage" class="text-danger"></span>
            </div>

            <div class="form-group">
                <button type="submit" class="ColorBtn">Save Changes</button>
                <a asp-action="Profile" class="BackBtnGray">Cancel</a>
            </div>
        </form>
    </main>
</div>

<link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css">
<link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />

<style>
    a {
        text-decoration: none;
    }

    .form-group label {
        display: block;
        margin-bottom: .5rem;
    }

    .select2-container .select2-selection--single {
        height: 36px !important;
        padding: 4px 8px !important;
        border-radius: 8px;
        font-size: 0.9rem;
    }

    .select2-selection__arrow {
        top: 5px !important;
    }

    .select2-container--bootstrap-5 .select2-selection {
        border-color: #ced4da;
    }

    .select2-container--bootstrap-5 .select2-results__options {
        max-height: 250px !important;
        overflow-y: auto !important;
    }

    /* --- التعديل الجديد: إجبار القائمة على الفتح لأسفل دائمًا --- */
    .select2-dropdown.select2-dropdown--above {
        top: 100% !important; /* اجبر الـ top إنه يكون تحت العنصر */
        bottom: auto !important; /* الغي أي محاولة لوضعه فوق */
    }
    /* --- نهاية التعديل --- */

</style>

<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script>

<script>
    $(document).ready(function () {
        $('.country-select').select2({
            width: 'resolve',
            theme: 'bootstrap-5',
            minimumResultsForSearch: 3,
            placeholder: 'Select your country...',
            allowClear: true,

            /* خلي دي زي ما هي علشان تضمن إن القائمة تترسم بره أي سكرول */
            dropdownParent: $('body')
        });
    });
</script>